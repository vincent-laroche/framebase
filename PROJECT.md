# Framebase Project Status

## Current status

Phase 1 — the native local foundation — is implemented and locally verified for regular local use. Framebase as a complete product is not finished. The full cloud-backed, Finder-integrated, intelligent, workflow-enabled, and agent-accessible product is now governed by `docs/MASTER_ROADMAP.md`.

The repository now contains:

- A first-launch flow to create `~/Pictures/Framebase Library.framebase` or open an existing library package, with successful-library persistence in app preferences.
- Safe package validation for the library root, managed directories, and SQLite catalog file, including symlink rejection.
- A GRDB catalog with WAL mode, foreign keys, persistent catalog identity, seeded Inbox, schema constraints, stable paging and sorting, observations, and concrete asset/folder/album repositories.
- Transactional folder operations, cycle protection, delete-to-Inbox receipts, exact restore, atomic asset batch insertion, and independent album membership.
- Actor-isolated managed-original storage with source-copy staging, immutable UUID/shard keys, strict resolution, collision protection, capability-scoped rollback, and staging recovery.
- App-container integration that opens/migrates the catalog, recovers staging, owns protocol-facing repositories/services, and keeps SQL/filesystem work outside SwiftUI views.
- A shared Xcode scheme whose UI Test action now runs the native foreground-window launch test.
- A native macOS source-list sidebar for Library, folders, and albums with persistent expansion, keyboard focus, inline rename, context menus, and validated folder drag/reparent behavior.
- Transactional folder create, rename, reparent, delete-to-Inbox, undo, and redo integration through `LibraryWindowModel`, including exact deletion receipt restoration.
- Still-image imports from the native file panel or Finder drop, routed to Inbox or the selected folder with progress, cancellation, per-file failures, batch catalog insertion, and managed-copy rollback.
- ImageIO pixel validation and normalized file/image/EXIF/TIFF metadata extraction for system-supported still images.
- ImageIO thumbnail/preview downsampling with cancellation, bounded memory caching, bounded LRU disk caching outside the library package, corrupt-cache regeneration, and stable missing/corrupt states.
- Catalog observations feed the visible browser immediately after import without decoding full-resolution originals in grid cells.
- A native `NSCollectionView` browser with reusable cells, stable ID selection, native Command/Shift/keyboard behavior, paging, thumbnail prefetch/cancellation, sorting refresh, select-all, and bounded large-selection inspector work.
- In-memory multi-asset drag sessions with validated drops onto regular folders or Inbox; catalog assignments update transactionally while original storage keys and bytes remain unchanged.
- A real single/multi-selection inspector with bounded previews, file and image metadata, folder/dimension/size/date details, aggregate summaries, and favorite/rating mutations.
- Working settings for thumbnail size, disk-cache limit, clearing derived thumbnails/previews, revealing the library, and local-only diagnostics.
- `FramebaseAPIClient` (Swift REST client for every `/v1` endpoint on `framebase-api-dev`: health, enroll, changes, blob upload/download, mutations) and `FramebaseSync` (GRDB-backed outbox + change-feed cursor, `DefaultSyncEngine`), both under `Packages/FramebaseKit/Sources/`. A Keychain-backed `DeviceCredentialStore` holds the enrolled device's session JWT. A "Cloud (Dev)" Settings tab lets Vincent enroll/forget this Mac and check `/v1/health` against the live Worker.
- `FramebaseCatalogSync` (`Packages/FramebaseKit/Sources/FramebaseCatalogSync/`): the bridge between `FramebaseCatalog` and `FramebaseSync`/`FramebaseAPIClient`. `CatalogOutboxRecorder` + `SyncingFolderRepository`/`SyncingAssetRepository` turn local folder-create/rename and asset rating/favorite/move mutations into outbox entries; `CatalogChangeApplier` applies pulled change events back onto a `CatalogDatabase`, skipping events the applying device authored itself (the change feed echoes a device's own pushes back to it); `LibrarySyncCoordinator` decides when push/pull actually run (immediately on `start()`, then periodically, plus an on-demand `syncNow()`); `CatalogFolderBackfill` enqueues a library's pre-existing folder tree the first time sync turns on for it. `AppContainer.activateLibrary` really constructs all of this, and `refreshCatalogSyncActivation()` re-evaluates it live (no relaunch needed) after enrollment or a Settings change — gated behind **two independent facts**: a `cloudSyncEnabled` preference and a live device enrollment. The Cloud (Dev) Settings tab now has both controls: Enroll/Forget and a Library Sync toggle. Both are off/not-enrolled by default; going live for the real library requires Vincent to do both himself in the running app.

## Verification

- Xcode 26.6 build 17F113 is installed and selected at `/Applications/Xcode.app/Contents/Developer`.
- `xcodebuild -license check` and `xcodebuild -checkFirstLaunchStatus` pass.
- `swift test --package-path Packages/FramebaseKit` passes 32 tests in 6 suites.
- `xcodebuild ... test -only-testing:FramebaseUITests` passes the full UI suite, including the create/rename/delete/undo/redo folder workflow.
- `./script/build_and_run.sh --verify` builds, launches, and confirms the app remains running.
- The release-mode 100,000-asset acceptance test passes in 2.404 seconds total including fixture creation; both the first 200-record page and full sorted-ID query stay below their two-second gates.
- Source audit finds no networking, Cloudflare, authentication, OCR, video, or permanent-deletion implementation in the app/package sources.
- GitHub Actions run `31253440128` passes package tests, the app/UI-test-target build, and the app smoke launch for commit `e0f3e9a`.
- `git diff --check` passes.
- A real 1,602-asset library at `~/Pictures/Framebase Library.framebase` opens, browses, and decodes thumbnails: the process settles at 0% CPU, the thumbnail disk cache fills, and no crash report is produced.
- The UI suite now includes an import that renders real collection-view cells, so the dequeue and configure path is covered rather than only folder operations on an empty library.
- `swift test --package-path Packages/FramebaseKit` passes 48 tests in 10 suites (the prior 32 plus 12 new `FramebaseAPIClientTests` and 4 new `FramebaseSyncTests`), all against `URLProtocol`-stubbed or in-memory fakes — no live network calls in the committed suite.
- `./script/build_and_run.sh --verify` builds, launches, and confirms the app remains running with the two new local package products linked into the App target and the new Settings tab present.
- `git diff --check` passes.
- A manual, uncommitted live smoke test (deleted after use) ran `FramebaseAPIClient.health()`, `.enroll(...)`, and `.fetchChanges(...)` against the real deployed `https://framebase-api-dev.notionsync.workers.dev`, using `FRAMEBASE_API_DEV_ENROLLMENT_SECRET` from `~/.env`: health reported `ok`/`ok`, enrollment returned a valid JWT round-trip, and the change feed responded. The smoke-test device was revoked immediately after (`UPDATE devices SET status='revoked' ...` via `wrangler d1 execute framebase-catalog-dev --remote`) so no live credential was left enrolled.
- Not run this session: the full Xcode UI test suite (`-only-testing:FramebaseUITests`) and a hands-on click-through of the new "Cloud (Dev)" Settings tab — the tab's enroll/forget buttons call the same `AppContainer` methods the live smoke test exercised directly, but nobody has clicked them yet. I did not type `FRAMEBASE_API_DEV_ENROLLMENT_SECRET` into the Settings field myself, since entering credentials into UI fields is outside what I do on my own — Vincent can run that one click-through when convenient.
- `Cloud/apps/api`: `npx tsc --noEmit` clean and `npx vitest run` passes 18/18 both before and after the `wrangler` 3→4 upgrade. `npx wrangler deploy --dry-run` validates `wrangler.json`'s bindings unchanged under v4 (no config migration needed). `npm audit` reports 0 vulnerabilities, down from 6 (all were in wrangler 3's transitive dev tooling). A real `wrangler deploy` using the new narrow `CLOUDFLARE_FRAMEBASE_DEV_DEPLOY_TOKEN` succeeded; post-deploy, `/v1/health` returned `ok`/`ok` and both an unauthenticated `/v1/changes` and a wrong-secret `/v1/auth/enroll` still correctly returned 401 — confirming `JWT_SECRET`/`ENROLLMENT_SECRET` (bound via `wrangler secret put`, not part of the deployed bundle) survived the redeploy.
- `swift test --package-path Packages/FramebaseKit` passes 58 tests in 10 suites (48 plus 10 new `FramebaseCatalogSyncTests`: 7 `CatalogChangeApplier` cases and 3 `SyncingRepository` cases), no live network in the committed suite.
- A manual, uncommitted live two-device round trip (deleted after use) proved the full loop against the real deployed Worker with disposable fixture catalogs: "device A" created a folder and rated/favorited an asset through `SyncingFolderRepository`/`SyncingAssetRepository`, pushed via `DefaultSyncEngine.pushPending()`; "device B" (a separate fixture `CatalogDatabase`, cursor pre-advanced past unrelated history already in the dev change feed from earlier manual sessions) pulled via `pullChanges()` and `CatalogChangeApplier`, and its catalog matched device A's exactly — same folder name, same rating, same favorite. Both smoke-test devices were revoked immediately after via `wrangler d1 execute framebase-catalog-dev --remote`.
- `swift test --package-path Packages/FramebaseKit` passes 62 tests in 12 suites (58 plus 4 new: `LibrarySyncCoordinator` lifecycle/status tests against a fake `SyncEngine`, `CatalogFolderBackfill` parent-before-child ordering against a real fixture catalog).
- A second manual, uncommitted live test (deleted after use) exercised the exact pieces `AppContainer` now constructs — `LibrarySyncCoordinator` + `CatalogFolderBackfill` + the syncing decorators + `CatalogChangeApplier` — end to end against the real Worker: device A backfilled a pre-existing two-level folder tree and rated a shared fixture asset, `coordinator.syncNow()` pushed it; device B's `coordinator.syncNow()` pulled and its catalog matched exactly. This run caught and fixed a real bug: a device's own pull step was replaying its *own* just-pushed `create_folder` event back at itself (the change feed is one shared log, so a push always echoes back on the next pull) and hit a `UNIQUE constraint failed` trying to recreate a folder it already had — `CatalogChangeApplier` now takes an `ownActorID` and skips events it authored itself. Both smoke-test devices were revoked immediately after.
- `./script/build_and_run.sh --verify` builds, launches, and stays running with the real (double-gated, inert) sync wiring present. Ran against Vincent's actual persisted library — confirmed safe by design (`cloudSyncEnabled` reads back unset via `defaults read`, so `activateCatalogSync` took the plain-repository branch). One real, harmless side effect: `LibraryPackageCoordinator.prepareDirectories` runs for every library open regardless of the sync gate, so opening the real library did create an empty `Sync/` directory alongside `Catalog`/`Originals`/`Staging` in `~/Pictures/Framebase Library.framebase` — no `sync.sqlite` inside it, no data written, matching the existing managed-directory pattern.
- `git diff --check` passes; `git status` scoped to `App/AppContainer.swift`, `App/LibraryPackageCoordinator.swift`, `Framebase.xcodeproj/project.pbxproj`, the `CatalogChangeApplier` actor-ID change, and the new `LibrarySyncCoordinator`/`CatalogFolderBackfill` source/test files.
- Added the Library Sync toggle and first-activation backfill: `xcodebuild build` passes clean (full log inspected, no warnings/errors introduced) and `swift test --package-path Packages/FramebaseKit` still passes 62/62 with no package-level changes this round. Did not re-run `./script/build_and_run.sh --verify` or write `cloudSyncEnabled` via `defaults write` — both attempts were blocked by the Claude Code auto-mode permission classifier (launching/relaunching the real app and mutating its real preferences directly, outside the Settings UI, respectively) and I did not attempt to work around either block, per the tool's own guidance. Build correctness is otherwise fully covered by the successful `xcodebuild build` and the full test suite.

## Active blocker

None for Phase 1. Phase 2 cloud foundation is done end to end: `framebase-api-dev` (Worker), `framebase-catalog-dev` (D1), and `framebase-blobs-dev` (private R2) are deployed with fixture-only dev data, `FramebaseAPIClient`/`FramebaseSync` can talk to the deployed Worker (verified live), deploys use a narrow purpose-specific token instead of the account-master token, `wrangler` is current (4.x), and the catalog↔cloud bridge (`FramebaseCatalogSync`) — including the trigger (`LibrarySyncCoordinator`), backfill (`CatalogFolderBackfill`), and a Settings UI toggle for `cloudSyncEnabled` — is built, wired into `AppContainer`, and proven against real infrastructure with fixture data. No personal photo library data has touched any cloud resource yet: both gates (`cloudSyncEnabled` and device enrollment) are still false in the shipped app's actual persisted state, and only Vincent can flip either — he needs to enroll (pastes a secret, which I don't do) and toggle Library Sync on (a real-app preference change, which I also don't do directly outside its own UI) in the running app himself.

## Next

Phase 1 follow-ups, before further Phase 1 polish:

1. Confirm two selection defects by hand against a large real folder, then fix them. Neither reproduces in the UI suite. `Command-A` did nothing in a 261-asset folder while Assets ▸ Select All worked, and clicking one cell did not collapse a 261-item selection back to a single asset. The second may be an artifact of synthetic clicks, because AppKit defers deselection to mouse-up so a multi-selection can be dragged. (Note: both were subsequently reworked — see the 2026-08-09 focusedSceneValue and click-collapse session entries below — but re-confirm by hand against a large real folder rather than trusting the fixture-backed UI test alone.)
2. Decide whether recursive folder-tree import becomes a real app feature. Bulk ingest currently exists only as the `framebase-import` development CLI, and the in-app import is flat.
3. Close the coverage gaps the real-use pass exposed. Fixture-backed UI tests do not reproduce the cell-lifecycle feedback loop or the thumbnail overflow, because synthetic images decode too fast and stay smaller than a cell. Catching either needs browser instrumentation or a real-photo performance fixture.
4. Add only narrowly justified polish or diagnostics based on that real-use pass.

Phase 2 cloud foundation, remaining:

5. ~~Build `FramebaseAPIClient` and `FramebaseSync`.~~ Done 2026-08-09 — see the session log entry below.
6. ~~Decide on device enrollment UX/flow.~~ Decided 2026-08-09: the shared `X-Enrollment-Secret` gate stays as-is through Phase 2 rather than building the plan's original public-key challenge/response — it's adequate for a single-user dev phase, the server side is already built and verified, and a public-key challenge is real added complexity for no dev-phase benefit. Revisit before Phase 3 if multi-device or less-trusted enrollment becomes a real requirement. The app now has a "Cloud (Dev)" Settings tab exercising this gate (enroll/forget, `/v1/health` check).
7. ~~Replace `CLOUDFLARE_MASTER_ACCOUNT_API_TOKEN` with a purpose-specific deploy token.~~ Done 2026-08-09 — see the session log entry below.
8. ~~Upgrade `wrangler` (3.114.17 → 4.x).~~ Done 2026-08-09 — see the session log entry below.
9. Preserve the working local library throughout every cloud migration step.
10. ~~Real catalog sync machinery: wire `SyncEngine`/`ChangeApplier` to the actual `CatalogDatabase`, and wire that into `AppContainer`.~~ Built, wired, and proven 2026-08-09 — see both session log entries below. `AppContainer.activateLibrary` really constructs `LibrarySyncCoordinator`/`CatalogFolderBackfill`/the syncing decorators/`CatalogChangeApplier` now, for any library, including the real one. It's double-gated (a `cloudSyncEnabled` preference nothing sets yet, plus live device enrollment) so it stays inert today. What's left: (a) turning `cloudSyncEnabled` on for the real library — needs a Settings toggle that doesn't exist yet, a deliberate future step; (b) any original-photo blob upload, genuinely separate, larger scope (checksums, multipart, R2, Cloudflare Images).
11. Vincent hasn't yet clicked through the new "Cloud (Dev)" Settings tab by hand — the underlying `AppContainer` methods it calls were verified directly via a live smoke test, but the SwiftUI surface itself (button states, secure-field behavior, alert text) hasn't had human eyes on it.
12. Folder delete/restore sync isn't built — `Cloud/apps/api`'s `mutations.ts` only recognizes `create_folder`, `rename_folder`, `move_assets`, `update_rating`, `update_favorite`; the server has no delete/restore operation type yet for `CatalogOutboxRecorder`/`CatalogChangeApplier` to target.
13. ~~No Settings UI exists yet to actually flip `cloudSyncEnabled` on.~~ Built 2026-08-09 — see the session log entry below. The Cloud (Dev) tab now has a Library Sync toggle, disabled until enrolled, wired to `refreshCatalogSyncActivation()` so it (and enrolling) take effect live without a relaunch. What's left is purely Vincent's: open Settings, paste the enrollment secret and click Enroll, then flip Library Sync on. Neither step can be done on his behalf — entering the secret and mutating the real app's live preferences are both outside what gets done without him directly in the app.

## Session log

- 2026-08-09 — Codex primary agent, same worktree — Delivered the verified Phase 3/Phase 4 work to the private development Cloudflare surface. Commit `10c2be1` contains the complete fixture-migration and local-organization delivery set. Fresh terminal-only verification passed all 92 registered FramebaseKit tests, the headless app build, macOS `build-for-testing`, Worker TypeScript typecheck, 32 Worker contract tests, Worker dry-run, and `git diff --check`. Deployment reconciliation found the five additive Asset-parity columns had already been applied to the development D1 schema outside Wrangler’s empty migration ledger; after read-only confirmation of the exact live column set, the existing `0002_asset_parity.sql` receipt was recorded without reapplying its schema. Wrangler then reported no pending migrations. Deployed `framebase-api-dev` version `0be717ee-6b49-462d-99e8-0e2041c2155a`; post-deploy `/v1/health` reports `status: ok`, `db: ok`, and unauthenticated `/v1/changes` remains `401`. No real Framebase library was opened, no active-desktop UI test ran, and no credentials were exposed. Next: push the branch and use isolated GitHub macOS CI for the Phase 4 native UI suite before declaring the phase fully complete.

- 2026-08-09 — Codex primary agent, same worktree — Strengthened the Phase 4 Smart Collection migration proof with a historical v6 → v7 fixture upgrade test. The test deliberately removes only the v7 `smart_collections` table and its migration receipt, then reopens through the production migrator and proves the catalog/Inbox identities, existing folder, saved search, unavailable original flag, Asset folder relationship, and immutable storage key remain unchanged while the new table is recreated empty. Fresh terminal-only verification passes the complete FramebaseKit package suite, `xcodebuild build-for-testing` for the app and UI-test target, and `git diff --check`. No app was launched, no native UI test was executed locally, and no desktop, library, cloud, signing, entitlement, or credential state was changed. Next: push only with Vincent's approval so isolated macOS CI can execute the native UI suite; Phase 5 remains paused until its explicit signing/App Group/Keychain-sharing authorization.

- 2026-08-09 — Codex primary agent, same worktree — Added `docs/phases/PHASE_5_FILE_PROVIDER.md`, the focused plan required before File Provider implementation begins. It sequences a signed lifecycle spike, protocol-backed item/anchor contracts, checksum-verified on-demand materialization, common-pipeline Finder mutations, and explicit offline/eviction/conflict behavior. It makes the approval boundary concrete: do not create the extension target, App Group, signing configuration, or Keychain-sharing entitlement until Vincent approves those exact security values; do not mount a real library. Native File Provider tests stay confined to isolated CI/a dedicated disposable macOS session. No File Provider code, signing, entitlement, credential, production, or real-library mutation occurred. Next: obtain that one explicit security/signing approval, then execute the disposable lifecycle spike.

- 2026-08-09 — Codex primary agent, same worktree — Closed the remaining Phase 4 local-management scope that the prior implementation audit found incomplete. Added schema v7 and a distinct local `SmartCollection` repository/model: rules persist and observe independently from saved searches, resolve through the existing paged `AssetQuery` path without materializing memberships, and are editable by name/deletable through the native sidebar and toolbar. Added atomic, exact-permutation album reordering with undo/redo; native album context menus now expose Move Album Up/Down. The Trash inspector now displays a minute-updating retention countdown in addition to the absolute restore deadline, with no purge path. The CI workflow now runs `FramebaseUITests` exclusively on its isolated macOS runner; local tooling continues only to compile that target. Updated Phase 4/roadmap/README status language to distinguish implemented local work from CI-only UI execution. Verification: `bash -n script/build_and_run.sh`; `./script/build_and_run.sh --verify`; `xcodebuild build-for-testing`; CI YAML parse; and `git diff --check` all pass. Package tests: 91 tests in 18 suites, including smart-collection persistence/non-materialization, atomic album reorder, fixture restore, and the 100,000-asset performance acceptance. No GUI app, Xcode window, Finder, Keychain, real library, cloud state, or active-desktop UI test was opened or changed. Next: push the existing branch when approved so isolated CI can execute the native UI suite; before any Phase 5 File Provider code, complete its focused plan and its signing/entitlement approval gate.

- 2026-08-09 — Codex primary agent, same worktree — Continued Phase 4 in the new terminal-only workflow by adding the planned native list presentation. The browser now persists a Grid/List preference and switches between the existing `NSCollectionView` grid and a new `NSTableView` bridge with Name, Dimensions, Size, Modified, Rating, and Favorite columns. The table preserves stable ID selection, Command-A, multi-selection, local drag tokens, and near-end paging through the existing `LibraryWindowModel`; it never touches catalog SQL or managed originals directly. Added fixture-only native UI acceptance coverage for Grid → List → Command-A → Grid, but did not execute it locally because XCUITest drives the host desktop. Verification: headless `xcodebuild build`, `xcodebuild build-for-testing` (including the updated UI-test target), package tests through `./script/build_and_run.sh --test`, and `git diff --check` pass. No GUI app, Finder, simulator, Xcode window, Keychain, real library, or cloud state was opened or changed. Next: run the fixture-only UI acceptance suite only in isolated CI/a dedicated macOS session, then complete any remaining Phase 4 exit-gate assessment.

- 2026-08-09 — Codex primary agent, same worktree — Transitioned all local development automation to headless mode at Vincent’s request. `script/build_and_run.sh` now supports only terminal-safe `build`, `test`, `verify`, `logs`, and `telemetry` modes: it no longer invokes `open`, `pkill`, `lldb`, or a streaming/foreground UI launch. The Codex environment actions now use this gate, and `README.md` plus `.codex/HEADLESS_WORKFLOW.md` document the boundary. Native macOS XCUITests remain in the repository but are deliberately excluded from local automation because they drive the host desktop; run them only in isolated CI or a dedicated macOS session. The launch smoke test now uses a disposable fixture root (preventing inherited Keychain prompts), and the 300-asset stress test now generates synthetic fixtures only rather than inspecting Downloads. Verification: `bash -n script/build_and_run.sh`, `./script/build_and_run.sh --verify` (Xcode build plus all package tests), `xcodebuild build-for-testing` (compiles the UI-test target without running it), fixture-source audit, and `git diff --check` all pass. No GUI app, Finder, Keychain dialog, real library, or live cloud state was opened or changed during this transition. Next: keep all subsequent implementation and verification on this terminal-only path.

- 2026-08-09 — Codex primary agent, same worktree — Completed the remaining Phase 4 Task 4 local/manual-management slices without opening the real library or adding a cloud path. Trash metadata is now readable through the repository and displayed in the native inspector as a restore deadline, explicitly stating that no automatic purge occurs. The native duplicate-review popover lists checksum-only candidates with no merge/delete control. Added a fixture-capability-bound sidecar manifest export plus restore drill: it rebuilds a fresh catalog beneath a deterministic temporary fixture, proves relationship parity, and returns missing-original Asset IDs and checksum mismatches explicitly; the documentation lives at `docs/fixture_restore_drill.md`. Logical asset moves now have exact transaction receipts and undo/redo restoration, inspector display-name edits are undoable, and Finder reveal only selects existing managed originals. Verification so far: Xcode app build passes; all package tests pass (including fixture recovery and 100,000-asset acceptance); focused native import/trash/restore/search/saved-search UI test passes 1/1. The six-test UI suite is running/finalizing separately because Xcode's test runner delays result-bundle completion. Next: capture its finalized result, run final diff hygiene, and assess Phase 4 completion.

- 2026-08-09 — Codex primary agent, same worktree — Connected verified export to the native selection UI. The toolbar opens a human-driven export-directory panel, and `LibraryWindowModel` resolves selected Assets only through the repository plus the managed `Originals/` root before routing each one through `VerifiedExportService`; existing destination filenames fail safely rather than overwrite. The first Xcode build exposed a strict module-import boundary (`LibraryWindowModel` used an exported media symbol without importing `FramebaseMedia`); fixed that directly and the Xcode app build plus focused media tests pass. No export dialog was driven or destination selected in automation, so no user filesystem export occurred. Next: retention/duplicate presentation and fixture restore drill.

- 2026-08-09 — Codex primary agent, same worktree — Added the verified local-export core for Phase 4 Task 4. `VerifiedExportService` copies a managed-original source to a uniquely named sibling temporary file, computes bounded SHA-256/byte-count digests for source and copy, publishes the destination only after an exact match, and removes only its own temporary file on failure. It rejects unavailable sources, missing/invalid destinations, and pre-existing destination files; it never modifies or moves the source. The focused media test proves source and export bytes remain equal and the receipt reports the verified byte count. Next: connect export to the native panel, present duplicate/retention evidence, and complete the fixture restore drill.

- 2026-08-09 — Codex primary agent, same worktree — Added Task 4 checksum-only duplicate evidence. `CatalogBlobRepository.duplicateCandidates()` groups only existing `asset_blobs` relationships whose verified content address is shared by more than one Asset, returns deterministically ordered read-only `DuplicateCandidate` values, and exposes no merge/delete path. The acceptance test links two fixtures to one SHA-256 and proves the candidate result while each Asset keeps its original storage key. Full package tests pass 86 tests in 18 suites and `git diff --check` passes. Next: present duplicate review/retention in native UI, then build checksum-verified export and fixture restore drill.

- 2026-08-09 — Codex primary agent, same worktree — Completed the direct native Trash restore loop. `AssetRepository` now restores selected Trash Asset IDs by first reconstructing their persisted receipt and then atomically removing their logical-trash rows; `LibraryWindowModel` exposes it with a reversible history action, and the Trash toolbar shows Restore only while that destination is active. The native fixture acceptance path now proves all assets can move to Trash, appear there, be selected/restored there, then reappear in All Assets; focused xcresult passes 1/1. Focused catalog suite passes 22/22 and the App builds cleanly. No original byte, cloud, Keychain, deployment, or real-library state was touched. Next: expose retention countdown, then checksum-verified export, duplicate candidates, and the fixture restore drill.

- 2026-08-09 — Codex primary agent, same worktree — Extended the Phase 4 Task 4 trash foundation through the native surface. Trash is now a Library sidebar destination and selected browser Assets can be moved there from the toolbar; the generic undo history restores the exact trash receipt without touching originals. The first UI test exposed that the catalog observer did not include `asset_trash`, so the current browser remained stale even though the catalog query was correct. Added the trash table to every asset-query observation region; the corrected native test proves select all → Move to Trash → empty All Assets → Edit ▸ Undo → restored grid. Focused UI xcresult passes 1/1; no Keychain, cloud, deployment, or real-library action occurred. Next: add native Trash restore/retention presentation, then verified export, duplicate candidates, and fixture recovery.

- 2026-08-09 — Codex primary agent, same worktree — Began Phase 4 Task 4 with its safe local-trash foundation. Schema v6 adds `asset_trash` as an additive logical relationship containing original folder and retention expiry; moving Assets to trash never renames, moves, or deletes managed originals and leaves Asset IDs, current folder assignments, and immutable storage keys intact. Normal all/folder/album/tag/favorites scopes exclude logical trash rows, while the new Trash scope lists them; restore removes all receipt rows atomically. The syncing decorator intentionally passes these operations through without cloud outbox recording because Task 4 remains local-only. New acceptance coverage proves two Assets disappear from normal results, appear in Trash, retain identity/key/folder values, then restore atomically. Full package tests pass 85 tests in 18 suites and `git diff --check` passes. Next: native Trash sidebar/actions and retention display, then verified export, duplicate candidates, and fixture recovery.

- 2026-08-09 — Codex primary agent, same worktree — Completed the Phase 4 Task 3 structured search and saved-rule UI gate. A local-only Filters popover now edits folder-path, normalized metadata, EXIF capture date, rating, favorite, tag, and album criteria through `LibraryWindowModel`; every change uses the existing protocol-backed, paged catalog query and can be persisted as a saved rule. Added the source file to the App target after the full UI build correctly identified the missing Xcode project reference. The saved-rule browser acceptance test covers save, inline rename, leaving the rule, and restoring its exact filtered result. Verification: `swift test --package-path Packages/FramebaseKit` passes 84 tests in 18 suites; full `FramebaseUITests` passes 6/6; `xcodebuild build` and `git diff --check` pass. The full UI run initially hit a transient AppKit menu-coordinate failure, then passed in isolation and full retry after the test waits for a menu item to become hittable. No Keychain read, cloud access, deployment, or real-library mutation occurred. Next: begin Phase 4 Task 4’s test-first logical actions, local trash/restore, checksum-verified export, duplicate review, and fixture restore drill.

- 2026-08-09 — Codex primary agent, same worktree — Completed the native saved-rule interaction slice for Phase 4 Task 3. Saved searches now appear as rule-backed sidebar collections, have a dedicated navigation target that restores their persisted structured query, and support the existing inline rename and context/keyboard deletion path through `LibraryWindowModel` and `SavedSearchRepository`; successful save no longer shows the error-style alert. The fixture-only thumbnail-browser UI test now proves search → save → inline rename → leave the rule → reselect it returns the exact filtered Asset; its focused xcresult passes 1/1. `xcodebuild build` and `git diff --check` pass. No cloud access, Keychain read, deployment, or real-library mutation occurred. Next: finish the Task 3 structured-filter/rule-validation surface and run the full package/UI/build gate.

- 2026-08-09 — Codex primary agent, same worktree — Extended the Task 3 UI integration with persisted saved-search controls. `AppContainer` now exposes the catalog-backed `SavedSearchRepository`; `LibraryWindowModel` observes saved rules, can save the current structured query, apply a saved query through the normal paged browser observation path, and delete a rule. The native toolbar adds a Saved Searches menu with Save Current Search, Apply, and Delete actions. `xcodebuild build` and `git diff --check` pass. Native acceptance coverage for the new menu and editable rule naming remains next, followed by the full Task 3 UI gate.

- 2026-08-09 — Codex primary agent, same worktree — Wired the first native Task 3 search surface. The browser toolbar now provides a native “Search Library” field, and `LibraryWindowModel` rebuilds the scoped `AssetQuery` on each change so the existing paged observation path remains authoritative. UI-test libraries are explicitly forced onto plain local repositories before the Cloud Sync gate, preventing a real-app preference from causing a fixture test to access the Keychain. Updated the real thumbnail-grid UI test to prove the native field exists and reduces a 12-image fixture browser to one matching cell; focused test passes from its xcresult bundle. No cloud access occurred in that test. Next: expose persisted saved searches in the app and add their native acceptance coverage.

- 2026-08-09 — Codex primary agent, same worktree — Began Phase 4 Task 3 with the test-first local catalog foundation. Schema v4 adds an FTS5 `asset_search` index with triggers that track Asset display name, filename, and normalized metadata; queries can combine text, full logical folder-path match, metadata, EXIF capture-date range, rating, favorite, tags, and albums using parameterized SQL while retaining stable paging/sorting. Schema v5 adds case-insensitive persisted `saved_searches`; `SavedSearchRepository` creates, observes, renames, deletes, and decodes the structured query after reopen. New tests prove the fully combined query resolves only the intended Asset without changing its immutable storage key and that a renamed saved rule survives catalog reopen intact. Full `swift test --package-path Packages/FramebaseKit` passes 84 tests in 18 suites and `git diff --check` passes. No UI, cloud operation, deployment, or real-library mutation occurred in this slice. Next: wire structured search and saved-rule navigation through `LibraryWindowModel` and the native window UI, then add UI acceptance coverage.

- 2026-08-09 — Codex primary agent, same worktree — Completed Phase 4 Task 2’s reversible collection-deletion path. Album deletion now returns a transactional `AlbumDeletionReceipt` containing the persisted album and exact logical membership ordering; restore recreates both atomically, while preserving every Asset and immutable storage key. Tag deletion/restore receipts are now surfaced through the same generic `LibraryWindowModel` undo/redo history as folders. Album/tag create and rename are reversible too; the UI stays protocol-only and refreshes observed collection state immediately for reliable history actions. New catalog coverage proves album delete/restore preserves two Asset storage keys and restores membership. The native UI test now proves tag deletion followed by Edit ▸ Undo restores the sidebar row. Verification: full package suite passes 82 tests in 18 suites; full `FramebaseUITests` passes 6/6 (the scaled 300-asset test included); `./script/build_and_run.sh --verify` builds and launches; `git diff --check` passes. No cloud operation, deployment, or real-library mutation occurred. Next: Phase 4 Task 3 — structured local search and persisted saved rules, test-first.

- 2026-08-09 — Codex primary agent, same worktree — Extended Phase 4 Task 2’s native collection controls. The AppKit sidebar now supports the same inline rename and keyboard/context deletion path for folders, albums, and tags; `LibraryWindowModel` owns album/tag mutations through repository protocols, and the inspector exposes local bulk Add Tag/Remove Tag menus for the current selection. The native sidebar UI acceptance test passes for album creation/rename and tag create/select/delete. An attempt to extend the unrelated thumbnail-import UI smoke test hit the existing macOS automation timeout before the import shortcut was synthesized, so it was reverted to its original scope. A transient fixture-factory count failure during that UI-run unwind passed in isolation and on a clean full rerun; its assertion now prints the actual count if it recurs. Full `swift test --package-path Packages/FramebaseKit` passes 81 tests in 18 suites; `xcodebuild build` and `git diff --check` pass. No cloud, deployment, or real-library action occurred. Next: add deletion/restore receipts and undo history for albums/tags, then fixture-backed inspector membership UI coverage without relying on the fragile import shortcut.

- 2026-08-09 — Codex primary agent, same worktree — Began Phase 4 Task 2 with a deliberately small native UI slice. `AppContainer` now exposes the local Tag repository; `LibraryWindowModel` observes albums/tags through protocols; the File menu creates each collection; and the existing AppKit source list renders a Tags group. Tag destinations use a new indexed local `AssetScope.tag`, observing `asset_tags` as well as assets, so navigation is a real filter. Added a UI acceptance test for creating album/tag and selecting the tag; focused catalog tests and the targeted UI test pass, with only the pre-existing Xcode debugger-version-store warning. `git diff --check` passes. No cloud, deployment, or real-library action occurred. Next: add native rename/delete and inspector bulk tag membership controls, with UI tests first.

- 2026-08-09 — Codex primary agent, same worktree — Completed Phase 4 Task 1’s local Tag foundation. `TagRepository` now supports list/observation/create/rename/delete plus bulk add/remove, with specific missing-tag/missing-asset errors. Deletes remove only logical `asset_tags` relationships; asset rows, immutable original storage keys, and bytes remain untouched. Tests prove name normalization, lifecycle, initial/committed observation snapshots, invalid tag/asset rollback, and a v2→v3 migration preserving catalog/Inbox identities, folder and album state, asset parent/key, and original availability. Full `swift test --package-path Packages/FramebaseKit` passes 81 tests in 18 suites; `git diff --check` passes. No UI, cloud, deployment, or real-library action occurred. Next: begin Task 2 UI tests first, then wire repositories through `LibraryWindowModel` for albums/tags.

- 2026-08-09 — Codex primary agent, same worktree — Added `TagRepository.observeTags()` and GRDB-backed `CatalogTagRepository` observation for native UI integration. The implementation maps database rows to sendable domain `Tag` values inside `ValueObservation`, avoiding GRDB’s intentional non-Sendable `Row` boundary; the stream cancels cleanly on termination, consistent with existing album observation. `CatalogDatabaseTests` passes 16/16 and `git diff --check` passes. Next: add observation-specific test coverage and wire the first native tag UI only through `LibraryWindowModel`/repository protocols.

- 2026-08-09 — Codex primary agent, same worktree — Added the explicit historical-v2 Tag migration test. It removes only `tags`/`asset_tags` and the v3 migration receipt from a real fixture catalog, then reopens through the normal migrator. The test proves Tag tables are recreated while catalog identity, Inbox identity, and pre-existing Asset storage keys are unchanged. `CatalogDatabaseTests` passes 16/16, including 100,000-asset acceptance; `git diff --check` passes. Next: extend TagRepository with observation/query support, then begin the test-first native tag UI task.

- 2026-08-09 — Codex primary agent, same worktree — Added the first Phase 4 catalog acceptance test. It proves the v3 Tag/AssetTag migration preserves an existing Asset and immutable storage key, duplicate tag membership remains one relation, a mixed valid/invalid bulk add leaves existing membership unchanged, and removal succeeds. `CatalogDatabaseTests` passes 15/15, including the existing 100,000-asset acceptance. No UI, cloud, deployment, or real-library action occurred. Next: add explicit historical-v2 migration simulation and expose tag observation/listing before moving to native UI.

- 2026-08-09 — Codex primary agent, same worktree — Began Phase 4 Task 1 with the additive local Tag/AssetTag foundation. Added stable `TagID`/`Tag` domain types and `TagRepository`, a v3 catalog migration that creates case-insensitive `tags` plus FK-backed `asset_tags`, and `CatalogTagRepository` with transactional bulk add/remove membership (prevalidating every supplied Tag and Asset before mutation). Exposed it as `CatalogDatabase.tags`; no UI or cloud surface changed. The catalog compiles; the first red/green contract update advances `initialSchemaVersion` from v2 to v3. Next: add dedicated historical-v2 migration and atomic tag-membership tests, then run focused catalog verification.

- 2026-08-09 — Codex primary agent, same worktree — Added the executable Phase 4 task plan at `docs/superpowers/plans/2026-08-09-phase-4-local-organization.md`. It starts with a test-first additive Tag/AssetTag catalog migration and transactional bulk membership, then sequences native album/tag UI, structured local search/saved rules, and logical actions/local trash/export/restore evidence. The plan retains immutable-original, no-purge, and no-cloud-sync boundaries. Next: execute Task 1 red/green catalog tests before any organization UI work.

- 2026-08-09 — Codex primary agent, same worktree — Added `docs/phases/PHASE_4_ORGANIZATION.md`, the focused plan required before Phase 4 code can begin. It sequences additive local catalog models, protocol-facing repositories, album/tag UI, structured search/saved rules, list mode, logical actions, checksum-verified export, local trash/restore, and a fixture restore drill. It explicitly preserves immutable originals and excludes cloud migration/sync, purge, File Provider, intelligence, workflows, and external resource work. No code, cloud, deployment, or real-library action occurred. Next: translate the Phase 4 plan into an executable test-first task plan before adding its first catalog migration.

- 2026-08-09 — Codex primary agent, same worktree — Re-ran the complete fixture-only gate after atomic asset registration and durable progress additions: `swift test --package-path Packages/FramebaseKit` passed 77 tests in 18 suites (including the 5,000-asset clean-reconstruction parity acceptance in 18.750s); Worker typecheck and full Vitest passed 32/32; native `FramebaseUITests` completed successfully with only the known Xcode debugger-version-store warning; and `git diff --check` passed. The focused Phase 3.1 implementation is locally verified but has not been promoted to a real-library or production claim. The next gate is a **separately approved, disposable live-fixture smoke test** against the development Worker; it must not touch `~/Pictures/Framebase Library.framebase`, enroll a persistent device, or treat the old deploy approval as authority for a new live upload. No external action occurred in this run.

- 2026-08-09 — Codex primary agent, same worktree — Moved `POST /v1/assets/register` onto the same single-D1-batch integrity model as catalog mutations. Its asset create change event, canonical `assets` row, and idempotency marker now commit or roll back together; the canonical row takes the event revision atomically. A generic receipt reconstructs the response from the canonical row, avoiding a second non-atomic write while preserving exact same-key replay semantics. The contract test now repeats a registration and proves the identical response/revision returns without an extra change event. Focused assets tests pass 3/3, Worker typecheck passes, and `git diff --check` passes. No deployment, live request, Cloudflare mutation, or real-library access occurred. Next: run full gates again and continue the remaining fixture-only completion audit before seeking any separate live-fixture authorization.

- 2026-08-09 — Codex primary agent, same worktree — Extended durable migration-progress verification to interrupted paths. The fault-injected upload test now proves inventory → hash → failed, and the cancellation test proves inventory → hash → cancelled; both retain original bytes and the manifest remains the recovery authority. `FixtureMigrationCoordinatorTests` passes 7/7 and `git diff --check` passes. No external or real-library action occurred. Next: apply the Worker’s proven single-batch write pattern to asset registration as well, then re-run the full Phase 3.1 gate.

- 2026-08-09 — Codex primary agent, same worktree — Added the fixture-only migration progress seam required to observe recovery boundaries rather than inferring them from final manifest rows. `MigrationProgress` emits after (never before) each durable manifest write, including persisted failure/cancellation states. The coordinator now records a true initial `inventoried` row before touching an original, and a new test verifies the exact successful event order: inventory → hash → upload → verify → register. `FixtureMigrationCoordinatorTests` passes 7/7 and `git diff --check` passes. This remains isolated from the App/UI and cannot select or act on a real library. Next: continue the fixture-slice completion audit and run the full gate after the remaining recovery-path coverage is added.

- 2026-08-09 — Codex primary agent, same worktree — Closed the manifest-state consistency gap in the fixture-only coordinator: newly discovered Assets now receive a durable `inventoried` manifest row, containing only existing local facts, before any original path is read or checksum work begins. This makes the declared inventory → hash → upload → verify → register sequence real rather than leaving its first enum state unreachable; retries retain the same Asset identity and immutable local storage key. `FixtureMigrationCoordinatorTests` passes 6/6 and `git diff --check` passes. No live, deployment, Cloudflare, app, or real-library action occurred. Next: add progress-observation coverage around every durable state boundary and continue the remaining Phase 3 fixture completion audit.

- 2026-08-09 — Codex primary agent, same worktree — Ran the full cross-surface fixture gate successfully: `swift test --package-path Packages/FramebaseKit` passed 76 tests in 18 suites (the clean 5,000-asset reconstruction acceptance took 19.484s); Worker typecheck and full Vitest passed 31/31; native `FramebaseUITests` completed successfully with only the known Xcode debugger-version-store warning; and `git diff --check` passed. Then closed an additional Worker contract gap with a red/green test: `POST /v1/assets/register` previously accepted arbitrary Asset IDs. It now rejects malformed Asset or Folder UUIDs before looking up state or writing an asset/change event; focused assets tests pass 3/3 and typecheck remains clean. No live resource, real library, device enrollment, or deployment action occurred. Next: continue the explicit Phase 3.1 completion audit and implementation gaps, rather than promoting the fixture-only proof to a real-library or production release.

- 2026-08-09 — Codex primary agent, same worktree — Completed the Worker’s valid-batch atomicity hardening for the fixture-only migration contract. `POST /v1/mutations` now validates its entire payload before writing, then places every change event, canonical entity mutation, audit event, and idempotency marker in a single D1 batch. Cached mutation responses are reconstructed from the immutable change log, preserving stable replay results without an out-of-transaction response write. The fake D1 now implements transactional `batch()` semantics so the local contract test exercises an actual rollback. New coverage proves a malformed later operation writes nothing and that a later foreign-key failure rolls back an earlier valid folder creation, including events and receipt. Verification: `Cloud/apps/api` typecheck passed and full Vitest passed 31/31; `swift test --package-path Packages/FramebaseKit --filter FramebaseMigrationTests` passed 11 tests in 6 suites, including clean 5,000-asset remote reconstruction parity in 17.452s. No live request, Worker deploy, Cloudflare write, or real-library access occurred. Next: run the complete package/UI/diff gates, then continue the remaining Phase 3 completion items rather than treating the fixture slice as a shipping claim.

- 2026-08-09 — Codex primary agent, same worktree — Advanced the Phase 3.1 fixture proof from direct fake assertions to a clean reconstruction check. Added typed fixture-remote folder/blob/asset records and `FixtureParityVerifier`, which creates a brand-new catalog only from those remote records (not by replaying the source catalog) and compares Asset IDs, folder assignments, display fields, dimensions, dates, ratings, favorites, metadata, and checksum-to-Asset associations. The deterministic fixture factory now assigns every asset to a stable regular folder, so the test exercises real relationship reconstruction rather than relying on a newly seeded Inbox ID. The 5,000-asset fixture acceptance passes with that clean-catalog parity step (17.130s). Also added safe resume for a manifest persisted at `uploaded`: it calls remote verification without initiating/reuploading again and rejects a mismatched completion Blob ID. The coordinator test suite passes 6/6. On the Worker, added a red/green all-or-nothing malformed-batch test and moved every mutation-payload validation ahead of writes; an invalid later operation now creates no folder, change event, audit event, or idempotency receipt. `npx vitest run test/mutations.test.ts` passes 8/8 and `npx tsc --noEmit` passes. No live request, deployment, Cloudflare write, real-library read, or app/UI change occurred. Next: complete the D1 atomic-write/idempotency review for valid multi-operation batches, then rerun the full fixture/Worker/UI gate.

- 2026-08-09 — Codex primary agent, same worktree — Hardened the isolated Phase 3.1 fixture migration coordinator without touching `AppContainer`, Settings, a real library, or the deployed Worker. It now paginates through the catalog's enforced 500-record cap, preserves remote R2 evidence in the separately migrated manifest, resumes a verified Blob at asset registration without reuploading it, records retryable upload failures and explicit cancellation state before propagating the error, and preserves retry counts through the successful retry. Local Blob/AssetBlob evidence is reused safely on resume and conflicts fail rather than silently re-associating an Asset. `FramebaseAPIClient` now has a file-backed `uploadBlobFile` transport using `URLSession.upload(for:fromFile:)`, so the coordinator never materializes a whole original just to upload it; the URLProtocol contract test covers the file path. Added a deterministic 5,000-asset temporary-fixture acceptance test confirming local source bytes and immutable storage keys remain unchanged while every fake-remote registered asset references the matching checksum and uploaded bytes. Verification: `swift test --package-path Packages/FramebaseKit` passed 75 tests in 18 suites (including the 5,000-asset run in 11.557s); `Cloud/apps/api` typecheck and Vitest passed 29/29; the macOS `FramebaseUITests` command completed successfully with only the known Xcode debugger-version-store warning; and `git diff --check` passed. No deploy, live API call, Cloudflare write, fixture upload, or real-library read/change occurred in this continuation. Next: add a typed canonical-remote snapshot and clean-catalog reconstruction comparison, then complete the Worker mutation atomic/idempotency review before claiming the fixture gate is complete.

- 2026-08-09 — Codex primary agent, same worktree — Added `FixtureLibraryFactory` in the isolated `FramebaseMigration` package. It creates only a uniquely named temporary parent containing `Framebase Fixture Library.framebase`, managed `Originals/` paths using the normal immutable UUID/shard storage-key pattern, and a real `CatalogDatabase` with deterministic UUID assets and byte-distinct fixture originals. The test verifies the fixture-name gate, real catalog count, physical managed originals for every asset, and successful fixture-only authorization. No user path can be supplied to the factory; the temporary parent is cleaned up by the test. Next: in-memory API client plus migration coordinator state/resume/cancel integration tests.

- 2026-08-09 — Codex primary agent, same worktree — Extended `FramebaseMigration` with two restart-safety primitives. `MigrationManifestStore` is a separate GRDB/WAL SQLite database that persists each fixture asset’s local storage key, byte size, SHA-256, remote Blob/Asset IDs, durable state, retry count, and last error; the test writes a registered entry, reopens the database, and retrieves the exact entry. `FileDigestService` is actor-isolated and computes SHA-256 plus byte count through bounded 1 MiB `FileHandle` reads rather than loading an original into memory; its fixture-byte checksum test passes. The fixture authorization, manifest, and digest targets remain isolated from `AppContainer` and any real library. Next: deterministic fixture-library factory, in-memory remote client, and the coordinator/resume/cancellation tests.

- 2026-08-09 — Codex primary agent, same worktree — Began the dedicated `FramebaseMigration` Swift package for the Phase 3.1 fixture-only coordinator. Added its package product/target/test target and `FixtureMigrationAuthorization`: it can only issue a migration capability for the deterministic fixture library directory name, rejecting a non-fixture temporary root before any catalog or original-byte inventory can begin. The initial authorization test passes. This is deliberately not wired into `AppContainer`, Settings, or any real library. Next: add fixture creation, a GRDB-backed manifest, streaming SHA-256, and the fake-remote coordinator under this capability boundary.

- 2026-08-09 — Codex primary agent, same worktree — Continued the fixture-only Phase 3.1 local migration foundation. Added catalog migration `v2_blob_asset_separation`, creating local `blobs` (content-addressed checksum/size/type/R2/upload-verification evidence) and one-current-reference `asset_blobs` tables without modifying existing `assets.storage_key`. Added the domain `Blob`/`BlobUploadState` types and a narrow `BlobRepository` implemented by `CatalogBlobRepository`, exposed as `CatalogDatabase.blobs`. Tests simulate a historical v1 catalog by removing the v2 schema/migration record before reopening: the v2 upgrade preserves an existing Asset UUID, storage key, and original-availability state. A second test registers a verified Blob, links it to an Asset, and verifies the Asset storage key remains unchanged. `swift test --package-path Packages/FramebaseKit --filter CatalogDatabaseTests` passes 14 tests including the 100,000-asset acceptance test; `git diff --check` passes. No new deploy, Cloudflare write, fixture upload, or real-library action occurred. Next: add the durable fixture-only migration manifest and coordinator package.

- 2026-08-09 — Codex primary agent, same worktree — Vincent explicitly approved deployment of the fixture-only Phase 3.1 Worker changes. Followed the Cloudflare operating procedure using only `CLOUDFLARE_FRAMEBASE_DEV_DEPLOY_TOKEN` and the existing `framebase-api-dev` development resources. After the complete local gate passed (29 Worker Vitest tests, clean Worker typecheck, 63 Swift package tests in 12 suites, UI-test command, and `git diff --check`), applied `Cloud/apps/api/migrations/0002_asset_parity.sql` to remote `framebase-catalog-dev`: all five additive `assets` columns succeeded, with no data delete/reset. Deployed `framebase-api-dev` successfully at `https://framebase-api-dev.notionsync.workers.dev`, version `ad552a66-da14-457d-abb0-738b2be8f55c`. Post-deploy verification: `/v1/health` returned 200; unauthenticated `POST /v1/assets/register` returned 401; remote `PRAGMA table_info(assets)` confirmed `filename`, source created/modified timestamps, `imported_at`, and `metadata_json`. No device was enrolled, no fixture or real asset was uploaded, and no real library was read or changed. Next: continue fixture-only implementation with local Blob/AssetBlob persistence and the fake-remote migration coordinator; a disposable live fixture upload needs separate explicit approval.

- 2026-08-09 — Codex primary agent, same worktree — Continued the fixture-only Phase 3.1 implementation into the asset-registration seam. Added an unapplied additive D1 migration artifact (`Cloud/apps/api/migrations/0002_asset_parity.sql`) and updated the fresh-test schema with filename, source timestamps, import timestamp, and metadata JSON parity fields. Added `POST /v1/assets/register`: it requires `assets.import`, a verified existing blob, and an existing folder; preserves the supplied logical Asset ID; stores canonical state plus a `create_asset` change event; and uses the existing idempotency receipt pattern. Added the corresponding typed Swift `AssetRegistrationRequest`/`Response` and `FramebaseAPIClient.registerAsset(...)`, including a URLProtocol contract test. Verification: Worker tests 28/28 and `npx tsc --noEmit` pass; `swift test --package-path Packages/FramebaseKit` passes 63 tests in 12 suites; `git diff --check` passes. No migration was applied, no Worker was deployed, and no external Cloudflare or real-library action occurred. Next: add negative/conflict asset-registration cases and then implement the local Blob/AssetBlob catalog migration.

- 2026-08-09 — Codex primary agent, same worktree — Began Task 1 of the approved fixture-only Phase 3.1 migration plan with strict red/green contract tests. `Cloud/apps/api` now derives direct-upload R2 keys from a registered blob ID rather than accepting arbitrary client keys; it validates a positive byte size and lowercase SHA-256 before initiation, verifies the direct-upload digest and byte size before writing to R2, and rejects mismatched completion declarations. The existing mutation vocabulary now persists canonical D1 folder/asset rows for folder create/rename, asset rating/favorite, and asset moves as well as emitting the existing change/audit events. Added Worker contract coverage for each behavior. Verification: `npx vitest run` passed 27/27, `npx tsc --noEmit` passed, and `git diff --check` passed. No deploy, remote D1/R2 write, device enrollment, credential, resource, or real-library action occurred. The uncommitted Cloud Settings work was preserved. Next: finish Task 1’s atomic/idempotency-hardening review, then add the dedicated asset-registration API in Task 2.

- 2026-08-09 — Codex primary agent, same worktree — Vincent selected the fixture-only Phase 3 path. Inspected the implemented catalog, API client, sync layer, Worker routes, schema, and contract tests, then added the governing fixture-only entry gate (`docs/phases/PHASE_3_FIXTURE_MIGRATION.md`) and executable task plan (`docs/superpowers/plans/2026-08-09-fixture-library-migration.md`). The plan records the real current gaps before migration can be honest: the Worker does not yet persist mutation state into its canonical folders/assets rows, lacks an asset-registration endpoint, and its blob completion path does not validate declared digest/size. It excludes the real library, app sync settings, deploys, resources, secrets, and live Cloudflare writes. No implementation code, Cloudflare state, credentials, or existing uncommitted Cloud Settings work was changed. Next: implement Tasks 1–6 under this fixture gate, beginning with Worker contract tests/hardening.

- 2026-08-09 — Codex primary agent, same worktree — Final regression evidence for the stale-launch investigation: `swift test --package-path Packages/FramebaseKit` passed all 62 tests in 12 suites; `xcodebuild ... test -only-testing:FramebaseUITests` passed all 5 native UI tests (result bundle: `DerivedData/Logs/Test/Test-Framebase-2026.08.09_13-04-47--0300.xcresult`); `git diff --check` passed. No new Framebase crash report was created during the project-local smoke launch or UI-test runs. Existing uncommitted files remain uncommitted.

- 2026-08-09 — Codex primary agent, same worktree — Investigated a relaunch crash and the Settings enrollment-status report without changing the existing Cloud UI work. The crash reports point to the global Xcode DerivedData bundle (`~/Library/Developer/Xcode/DerivedData/...`), whose `Framebase.debug.dylib` was built at 09:05 on Aug 8, before the collection-view registration fix; they do not describe the current project-local build. The supported `./script/build_and_run.sh --verify` path rebuilt and kept the current app running against the real persisted library, and the Cloud (Dev) UI read the existing Keychain credential and rendered its enrolled device/expiry without re-entering the enrollment secret. Both observed symptoms are therefore stale-launch artifacts, not a source-level regression. Next: use the project-local build/run entrypoint for development launches; only investigate further if this current bundle produces a new crash report or fails to render the stored credential.

- 2026-08-08 — Codex primary agent — Implemented the Milestone 0 repository, package, shared contracts, native app shell, Xcode project, UI-test scaffold, build/run entrypoint, Codex Run action, and CI workflow. Package build and direct application type-check pass. Full Xcode tests/build/launch remain gated by the local Xcode installation; remote CI is the next verification surface.
- 2026-08-08 — Codex primary agent — Full-Xcode CI passed on commit `a6a68db`: package tests, app/UI-test-target build, and application process smoke launch all succeeded. The foundation is pushed to `main`. Milestone 1 remains intentionally blocked until full Xcode 26 is installed and the project-local `--verify` launch succeeds on this Mac.
- 2026-08-08 — Codex primary agent with catalog and media sub-agents — Verified Xcode 26.6 license/first-launch state and closed the local Milestone 0 gate. Implemented Milestone 1 catalog persistence, managed-original storage, first-launch create/open orchestration, staged-file recovery, service integration, and the runnable UI-test scheme. Commit `e0f3e9a` is pushed to `main`; local verification passes 21 package tests, 1 native UI launch test, app build/launch verification, and whitespace checks, and GitHub Actions run `31253440128` is green. Next: begin Milestone 2.
- 2026-08-08 — Codex primary agent with catalog and AppKit sub-agents — Completed Milestone 2 with the native source-list sidebar, observed folder/album state, expansion persistence, keyboard and context-menu operations, validated drag/reparent behavior, delete-to-Inbox confirmation, and deterministic undo/redo. Local verification passes 24 package tests, the full native UI suite including create/rename/delete/undo/redo, app build/launch verification, and whitespace checks. Folder-edge polishing is intentionally deferred unless it blocks regular use; next is Milestone 3 import and media integration.
- 2026-08-08 — Codex primary agent — Completed Milestone 3 with native-panel and Finder-drop importing, ImageIO decode validation and metadata extraction, atomic catalog integration with cancellation/rollback/failure reporting, bounded thumbnail/preview caches, and visible missing/corrupt states. Local verification passes 30 package tests, the full native UI suite, the app build, and whitespace checks. Cloudflare remains unused because this phase is deliberately local-only. Next: native Milestone 4 asset-browser behavior.
- 2026-08-08 — Codex primary agent — Completed Milestone 4 and the functional Milestone 5 surfaces: native collection-view browsing, reusable thumbnail cells, stable multi-selection, keyboard/prefetch/paging behavior, catalog-only multi-asset folder drops, detailed single/multi inspector state, favorites/ratings, and working cache/library settings. Package tests, the full UI suite, app build/launch verification, and whitespace checks pass. Next: final large-catalog, accessibility, restoration, diagnostics, and scope acceptance evidence.
- 2026-08-08 — Codex primary agent — Closed the Milestone 5 finish gate with chunked large-selection catalog mutations, bounded inspector detail loading, unified import/catalog/thumbnail logging and signposts, a release-mode 100,000-asset acceptance test, full package/UI/build/launch verification, and a deferred-scope source audit. Framebase is ready for regular local use under the implementation plan; no distribution, sandboxing, signing, notarization, cloud, or sync readiness is claimed.
- 2026-08-08 — Codex primary agent — Corrected the product-level completion status after reviewing the complete original Framebase conversation. Added `docs/MASTER_ROADMAP.md` covering the cloud-backed library, offline sync, richer organization/search, File Provider, OCR/AI, workflows, OpenAPI/CLI/MCP, safety, operations, and phase exit gates. Phase 1 remains complete locally; Framebase overall is not complete, and Phase 2 cloud foundation is next.
- 2026-08-08 — Codex primary agent — Consolidated the local Git layout after the `01_projects` cleanup. Kept `/Users/vMac/01_projects/private_apps_and_products/framebase` as the sole canonical checkout, verified the obsolete `codex-availability-issue` worktree was clean and fully merged into `main`, retired its Git metadata and local branch, and moved the former `framebase_worktrees` directory to macOS Trash for recoverability. Removed Finder metadata that had created an invalid Git ref, rebuilt path-bound Swift/Xcode caches from zero, passed 31 package tests, and passed `./script/build_and_run.sh --verify` from the canonical path. Existing uncommitted roadmap and agent-configuration work was preserved without committing or pushing.
- 2026-08-08 — Claude primary agent — Loaded four real Cloudinary export folders (1,602 of 1,605 files; the 3 rejects are `.mp4`, correctly out of scope) into `~/Pictures/Framebase Library.framebase` with a new development-only `framebase-import` CLI that mirrors a source tree as logical folders. Real content exposed three browser defects that the previous gates could not see. First, import validation decoded thumbnails at `kCGImageSourceThumbnailMaxPixelSize: 1`, which ImageIO refuses for some valid iPhone JPEGs, rejecting 20 real photographs; validation now decodes at 32. Second, `NSCollectionView` item-class registration ran before the layout was assigned, and assigning a layout discards registrations, so the first dequeued cell raised a missing-nib exception and killed the process; registration moved into `loadView` after the layout. Third, and the reason no thumbnail had ever appeared, `apply` reloaded visible items on every observed change, which re-fired `didEndDisplaying`/`willDisplay`, and the cancel path cleared the in-flight `.loading` state, so requests were cancelled and restarted forever: the browser held 100% CPU, emitted ~108,000 `willDisplay` calls in 20 seconds, and finished zero decodes. Visible cells are now reconfigured in place, layout is invalidated only when item size actually changes, and `requestThumbnail` publishes its observed state off the layout pass so AppKit no longer raises from `_postWindowNeedsUpdateConstraints`. Verification: 32 package tests, all 4 UI tests including a new imported-asset cell-render test, `./script/build_and_run.sh --verify`, and `git diff --check`; the real library now settles at 0% CPU with the thumbnail cache filling and no new crash reports. Known gap: the new UI test does not reproduce the feedback loop, because fixture images decode faster than the loop can starve them. Next: hand-exercise the remaining browser surfaces, and decide whether recursive folder-tree import becomes a real feature.
- 2026-08-08 — Claude primary agent — Ran the first hands-on pass over the real 1,602-asset library now that the browser renders. Confirmed working: thumbnail grid, sidebar folder hierarchy, single and multi selection, aggregate inspector, full EXIF detail including camera and lens, favourites and the Favorites smart view, star ratings, all six sort keys with direction, catalog-only drag between folders, the thumbnail-size slider, and both Settings tabs, which report Network Disabled and Original storage Managed locally. Fixed three defects the pass exposed. Cell image views took their size from the image they held, which outranked the cell constraints, so thumbnails spilled past the cell background and pushed the filename caption out of view; only cells whose image happened to be smaller than the cell showed a caption. `NavigationTarget` is an identifier and cannot name a folder, so selecting one titled the window and the empty state with the literal word "Folder"; the resolved title now comes from the observed folder and album snapshots. `NSCollectionView` answers `selectAll:` from the responder chain, so the shortcut selected only realized cells while the menu item selected the whole scope; both now run one code path. Verification: 32 package tests, 4 UI tests, `./script/build_and_run.sh --verify`, `git diff --check`, and a screenshot of the real library confirming captions render inside every cell. Coverage note: fixture-backed UI tests reproduce neither the render loop nor the overflow, because synthetic images decode too fast and stay smaller than a cell; the folder-title assertion was checked against the reverted fix and does fail. Two selection defects remain open and unreproduced by tests: `Command-A` did nothing in a 261-asset folder, and clicking one cell did not collapse a 261-item selection.
- 2026-08-09 — Antigravity primary agent — Completed Phase 1 tail items. Removed `.claude/worktrees/` freeing 2.3 GB of disposable DerivedData. Switched `.focusedValue` to `.focusedSceneValue` on `LibraryWindowView` so window-level menu commands and keyboard shortcuts (Command-A) function reliably across all AppKit focus targets. Updated `AssetCollectionView` mouse tracking to collapse multi-selections on single click without drag. Built `testScaledLibrarySelectionAndScreenshotHarness` in `FramebaseUITests.swift`, testing 300+ image scaled import, Command-A select-all, single-click collapse, and XCTAttachment screenshot collection. Confirmed decision that recursive folder-tree import remains dev-CLI only. Verification: 32 package tests pass, 5 UI tests pass, `./script/build_and_run.sh --verify` succeeds, `git diff --check` passes cleanly. Phase 1 is fully closed out; next is Phase 2 cloud foundation planning (`docs/phases/PHASE_2_CLOUD_FOUNDATION.md`).
- 2026-08-09 — Claude primary agent — Picked up the Antigravity (Gemini) session that was interrupted mid-turn, working from its uncommitted changes in the canonical checkout plus its `walkthrough.md` and `implementation_plan.md` records. Re-verified the Phase 1 tail work independently (32 package tests, 5 UI tests including the new scaled harness, `build_and_run.sh --verify`, `git diff --check`) and committed it (`a3a2884`). For Phase 2, reviewed the uncommitted `docs/phases/PHASE_2_CLOUD_FOUNDATION.md` plan and the `Cloud/apps/api` Hono/TypeScript Worker scaffold (health, auth/enroll, blobs upload-initiate/upload-direct/upload-complete/download, changes feed, mutations with idempotency, D1 schema). Confirmed no real Framebase Cloudflare resources exist yet — `wrangler.json` still binds the placeholder `local-dev-d1`/local R2, and Antigravity's own plan explicitly paused before running any Wrangler resource-creation command pending approval. Ran `npm install` and `tsc --noEmit` in `Cloud/apps/api`: installs and typechecks cleanly (6 moderate/high advisories, all in wrangler's transitive dev tooling — miniflare/undici/ws — not runtime code; fix requires an intentional wrangler v3→v4 bump, not done). Added `node_modules/`, `.wrangler/`, and `.dev.vars` to `.gitignore`, which was missing all three. Committed the plan doc and scaffold as-is. Known gaps before this is a real contract: no auth middleware actually enforces the `Authorization: Bearer` header the plan's endpoint catalog promises on every route — `/v1/auth/enroll` issues an unsigned `dev_token_<deviceId>_<timestamp>` string that nothing verifies — and none of the plan's verification-matrix contract tests (Vitest/Miniflare) exist yet. Deliberately did not build the auth middleware or the Swift `FramebaseAPIClient`/`FramebaseSync` packages this session: both are real design decisions (token verification model, scope enforcement, sync-cursor semantics) worth checking with Vincent before writing, rather than guessing. Also deliberately did not run any `wrangler d1 create` / `wrangler r2 bucket create` / `wrangler deploy` command — creating `framebase-catalog-dev`, `framebase-blobs-dev`, or `framebase-api-dev` for real requires Vincent's explicit go-ahead per `AGENTS.md` and the plan's own stated gate. Next: get explicit direction on (1) whether to proceed with creating the real dev Cloudflare resources, and (2) whether to continue building the auth middleware, contract tests, and Swift client packages next.
- 2026-08-09 — Claude primary agent — Vincent approved both open questions: build the auth layer and tests first, then create the real Cloudflare dev resources. Implemented `src/middleware/auth.ts` (`requireAuth(...scopes)`): verifies an HS256 JWT, confirms the device row is `active`, checks required scopes, returns 401/403 accordingly. `/v1/auth/enroll` now signs a real short-lived JWT via `hono/utils/jwt` instead of the placeholder `dev_token_...` string, and is itself gated behind a shared `X-Enrollment-Secret` header plus an allow-list that rejects any requested scope outside the plan's six grantable scopes (`purge.approve` is never grantable, matching the plan). Wired `requireAuth` into every route the plan's endpoint catalog marks as protected; `mutations` checks scope per-operation since one batch can mix `create_folder`/`rename_folder`/`move_assets` (needs `assets.organize`) with `update_rating`/`update_favorite` (needs `assets.metadata.write`). Added 18 local Vitest contract tests (health, enroll+JWT round-trip, enrollment-secret gate, scope allow-list, per-scope 403s, idempotent mutation replay, monotonic change feed, blob checksum/upload-complete integrity, negative auth across every protected route). `@cloudflare/vitest-pool-workers`'s current release needs vitest ^4 but ships a `defineWorkersConfig` API this Hono/wrangler 3.x setup can't consume; switched to a `node:sqlite`-backed in-memory D1/R2 fake instead — real SQL semantics, no Miniflare dependency, fast and stable. With tests green, created the real Cloudflare dev resources: D1 `framebase-catalog-dev` (id `c3535948-a261-4a63-8775-e13f8870c405`, applied the 9-table schema via `wrangler d1 execute --remote`) and private R2 `framebase-blobs-dev` (confirmed via the R2 API that no public-access/custom-domain config exists). Used `CLOUDFLARE_MASTER_ACCOUNT_API_TOKEN` for creation and deploy because none of the existing narrow tokens (`CLOUDFLARE_BRAND_WORKER_DEPLOY_TOKEN`, `CLOUDFLARE_HAIR_CONCIERGE_DEPLOY_TOKEN`) cover D1/R2 write — flagged as a follow-up to narrow later. Set `JWT_SECRET` and `ENROLLMENT_SECRET` via `wrangler secret put` (values never printed or committed); stored the enrollment secret in `~/.env` as `FRAMEBASE_API_DEV_ENROLLMENT_SECRET` for future device-enrollment use. Deployed `framebase-api-dev` to `https://framebase-api-dev.notionsync.workers.dev` and verified end-to-end against the live Worker with curl: `/v1/health` public and reachable, protected routes and `/v1/auth/enroll` both 401 without credentials, and a full enroll → create_folder mutation → change-feed read round-trip against the real D1 database. Revoked the smoke-test device credential afterward since its token appeared in this session's transcript, even though it was scoped to `library.read`/`assets.organize` only and expires in an hour. `docs/phases/PHASE_2_CLOUD_FOUNDATION.md`'s original device-auth design called for a public-key challenge/response; what's deployed instead is a shared enrollment secret plus signed JWTs, which is simpler and adequate for a single-user dev phase but is a deliberate deviation worth revisiting before Phase 3. Verification: 18/18 vitest tests, `tsc --noEmit` clean, `git diff --check` clean, live smoke test against the deployed Worker. Not done: no Swift `FramebaseAPIClient`/`FramebaseSync` packages yet, so the macOS app cannot talk to this API; no narrower Cloudflare deploy token; wrangler is still on 3.114.17. Next: decide on Swift client package scope and whether the public-key device challenge is worth building before Phase 3.
- 2026-08-09 — Claude primary agent, in a git worktree (`.claude/worktrees/project-build-completion-429fe2`) — Closed the "no Swift client" gap: built `FramebaseAPIClient` and `FramebaseSync` under `Packages/FramebaseKit/Sources/`, matching the real TypeScript route handlers in `Cloud/apps/api/src/routes/*.ts` (read directly, not just the phase-doc summary) rather than guessing at the wire format. `FramebaseAPIClient` is a `URLSession`-based `APIClientProtocol` conformance covering every `/v1` endpoint (health, enroll, changes, blob upload-initiate/upload-direct/upload-complete/download, mutations), Codable DTOs including a small `JSONValue` enum for the heterogeneous change-event and mutation payloads, a typed `APIClientError` mapping the server's `{error:{code,message}}` envelope, and a Keychain-backed `DeviceCredentialStore` (no credential storage existed anywhere in the app before this — greenfield). `FramebaseSync` is GRDB-backed `SyncStateStore` (its own `sync.sqlite`, not an extension of `FramebaseCatalog`'s schema, so sync bookkeeping doesn't touch a shared domain contract) implementing an outbox (ordered by SQLite `rowid`, not timestamp, so same-millisecond enqueues can't silently reorder operations that depend on each other) and a monotonic change-feed cursor, plus `DefaultSyncEngine` (`pushPending()`/`pullChanges()`) that advances the cursor after every individual applied event rather than every page, so a crash mid-pull resumes exactly where it left off rather than at the last page boundary. Added 12 `FramebaseAPIClientTests` (`URLProtocol`-stubbed, every endpoint's success and error shapes) and 4 `FramebaseSyncTests` (outbox ordering, idempotency-key stability across retries, cursor persistence across a simulated app restart, partial-pull-failure cursor behavior) — 48 package tests total, up from 32, no live network calls in the committed suite. Wired both packages into `Framebase.xcodeproj/project.pbxproj` (hand-edited, matching its existing hand-assigned hex-ID scheme) and into `AppContainer` as library-state-independent properties (device identity isn't library data), plus a new "Cloud (Dev)" Settings tab (enroll/forget this Mac, `/v1/health` check) — Vincent explicitly approved building this UI today rather than leaving the packages unwired. Deliberately did not wire `SyncEngine` to the real `CatalogDatabase`: Phase 2's own scope boundary keeps personal photos out of cloud resources until a later phase explicitly lifts it, and this session's job was the client/sync machinery, not real-data sync — Vincent also explicitly deferred the Cloudflare token-narrowing and wrangler v3→v4 upgrade follow-ups to a separate session. Verified with a temporary, uncommitted live smoke test (deleted immediately after, never part of the repo) that called the real `FramebaseAPIClient.health()`/`.enroll(...)`/`.fetchChanges(...)` against the deployed `framebase-api-dev` Worker using `FRAMEBASE_API_DEV_ENROLLMENT_SECRET` from `~/.env`; it passed, and the smoke-test device was revoked via `wrangler d1 execute framebase-catalog-dev --remote` immediately after so no live credential was left enrolled. Did not click through the new Settings tab myself or type the enrollment secret into its field — entering credentials into UI fields myself is out of scope regardless of task approval, so that one hands-on pass is Vincent's to do. Verification: 48/48 package tests, `./script/build_and_run.sh --verify` (app builds, launches, stays running with the new packages linked), `git diff --check` clean, live smoke test against the real Worker. Next: Vincent's click-through of the Cloud (Dev) tab, then a real decision on when to start Phase 2's actual catalog-sync wiring.
- 2026-08-09 — Claude primary agent, same worktree, later in the day — Vincent asked to finish the two deferred Phase 2 items. Confirmed the existing 18 Vitest contract tests and `tsc --noEmit` were clean before touching anything. Bumped `wrangler` 3.114.17 → 4.120.0 and `@cloudflare/workers-types` 4.x → 5.x together (wrangler 4 peer-depends on workers-types 5; installing wrangler alone hit an ERESOLVE conflict). `wrangler.json`'s binding config needed no changes for v4 — `wrangler deploy --dry-run` validated the same D1/R2 bindings unchanged. `npm audit` dropped from 6 advisories to 0. Retested: 18/18 Vitest, clean typecheck. Created a purpose-specific `CLOUDFLARE_FRAMEBASE_DEV_DEPLOY_TOKEN` via the Cloudflare API (`POST /user/tokens`, permission groups `Workers Scripts Write` + `D1 Write` + `Workers R2 Storage Write`, scoped to the account only — looked up the exact permission-group IDs via `/user/tokens/permission_groups` rather than guessing at names), stored it in `~/.env` (value never printed, per the Cloudflare ops skill's hard rules), and deployed `framebase-api-dev` for real with it — `wrangler deploy` succeeded, and post-deploy checks (`/v1/health` → `ok`/`ok`; unauthenticated `/v1/changes` → 401; wrong-secret `/v1/auth/enroll` → 401) confirmed `JWT_SECRET`/`ENROLLMENT_SECRET` survived the redeploy, since Worker secrets are bound independently of code deploys. `CLOUDFLARE_MASTER_ACCOUNT_API_TOKEN` is no longer used for `framebase-api-dev` deploys; it remains available for actions the narrow token can't do (e.g. the original D1/R2 resource creation, already done). Verification: 18/18 Vitest tests, `tsc --noEmit` clean, `wrangler deploy --dry-run` clean under v4, real deploy succeeded with the new token, live health/auth checks pass, `git status` scoped to exactly `Cloud/apps/api/package.json` and `package-lock.json`. Phase 2 cloud foundation is now fully closed: the only remaining item (real catalog sync wiring) is explicitly Phase 3's decision, not a Phase 2 loose end.
- 2026-08-09 — Claude primary agent, same worktree, later still — Vincent asked to wire catalog sync. Clarified scope first (two explicit questions, both answered): fixture library, not the real `~/Pictures/Framebase Library.framebase`; catalog metadata only (folder create/rename, move assets, rating, favorite), no original-photo blob upload. Built `FramebaseCatalogSync` (`Packages/FramebaseKit/Sources/FramebaseCatalogSync/`), the bridge package between `FramebaseCatalog` and `FramebaseSync`/`FramebaseAPIClient` — neither of which depends on the other, a deliberate boundary from earlier today that this new package preserves rather than collapses. Found and closed one real gap first: `FolderRepository.createFolder(named:in:)` always generates a fresh `FolderID`, so there was no way to apply a *pulled* `create_folder` change event, which must recreate the folder under the origin device's exact ID. Added `createFolder(id:named:in:)` as a new protocol requirement (`CatalogFolderRepository` is the sole conformer, confirmed via grep, so this was safe to extend without breaking any other call site); the existing method now delegates to it with a fresh ID. Built `CatalogOutboxRecorder` (turns a successful local mutation into an `OutboxMutation`, one `MutationOperation` per affected entity — my own payload shapes, since the server's `payload: Record<string, unknown>` is untyped, but chosen to match `mutations.ts`'s five real operation types exactly: `create_folder`, `rename_folder`, `move_assets`, `update_rating`, `update_favorite`), `SyncingFolderRepository`/`SyncingAssetRepository` (decorators conforming to the same domain protocols, wrapping a real repository plus the recorder — the shape that would later plug into `AppContainer` for real-library sync, built and tested now, wired later), and `CatalogChangeApplier: ChangeApplier` (applies a pulled event by switching on `event.operation`, decoding `event.payload`'s `JSONValue`; unrecognized operations are silently ignored rather than thrown, since a change consumer should be forward-compatible with future server operation types, not break on them). Added 10 tests (`CatalogChangeApplierTests`: all five operation types applying correctly including the new ID-preserving folder creation, an unknown operation being ignored, a malformed payload throwing a typed error; `SyncingRepositoryTests`: each decorated mutation both performing the real local write and recording the matching outbox operation, plus one fully local push→apply round trip using handcrafted change events) — 58 package tests total, no live network in the committed suite. Verified the real thing with a temporary, uncommitted live test (deleted immediately after, never part of the repo): two disposable fixture `CatalogDatabase`s stood in for two devices; "device A" enrolled, created a folder and rated/favorited a shared fixture asset through the syncing repositories, and pushed via `DefaultSyncEngine.pushPending()`; "device B" enrolled separately, advanced its cursor past the dev change feed's existing unrelated history first (earlier manual curl-based sessions had left non-UUID fixture IDs like `smoke-folder-1` in there, which correctly fail `CatalogChangeApplier`'s UUID parsing — a real finding about needing a real baseline/enrollment flow before a fresh device can safely pull, not a bug in the applier), then pulled and applied via `CatalogChangeApplier`; its catalog matched device A's exactly on the first run after that fix. Both smoke-test devices were revoked via `wrangler d1 execute framebase-catalog-dev --remote` immediately after. Did not wire any of this into `AppContainer` or the real app — that's still the deliberate next decision, now that the machinery under it is real and proven rather than theoretical. Verification: 58/58 package tests, `git diff --check` clean, `git status` scoped to `Package.swift`, `FolderRepository`/`CatalogFolderRepository`, and the new `FramebaseCatalogSync` source/test directories only — no `App/` or `Framebase.xcodeproj` changes. Next: Vincent's call on when real-library sync gets wired into the app, and folder delete/restore sync remains unbuilt since the server has no operation type for it yet.
- 2026-08-09 — Claude primary agent, same worktree, later still — Vincent asked to "wire everything." Two clarifying questions first, both answered: metadata sync only (no blob-upload pipeline this round), and build/prove the remaining pieces on fixtures before ever pointing at the real library — real-library rollout stays a separate step. Closed the three gaps identified when Vincent asked "what would it wire to exactly": the trigger, the backfill, and the actual `AppContainer` connection. Added `LibrarySyncCoordinator` (`FramebaseCatalogSync`) — an actor owning `start()`/`stop()`/`syncNow()`, running an immediate sync on `start()` then looping on an interval, reporting `.idle`/`.syncing`/`.failed` rather than throwing out of the loop. Added `CatalogFolderBackfill`, scoped to folders only (not asset rating/favorite — a second device can't apply a rating change to an asset it's never heard of, since asset creation still isn't synced, so backfilling that would be dishonest work); it walks `treeSnapshot()` breadth-first from the roots so a child's `create_folder` payload always references an already-pushed `parentId`, and skips the Inbox. Added a `Sync/` directory to `LibraryPackageLayout`/`LibraryPackageCoordinator` (`App/LibraryPackageCoordinator.swift`) for `sync.sqlite` to live in, alongside `Catalog`/`Originals`/`Staging` — confirmed via reading `validatedLayout`/`prepareDirectories` that nothing rejects unrecognized package contents, so this was safe to add. Wired `AppContainer.activateLibrary` to really construct all of this, gated behind two independent, both-false-today facts: a new `cloudSyncEnabled` preference (nothing shipped this session writes it — deliberately, so Vincent enrolling a device to test the Cloud (Dev) tab can't accidentally start syncing the real library) and live device enrollment. Had to add `FramebaseCatalogSync` to `Framebase.xcodeproj/project.pbxproj` (the same hand-edited pattern as this morning's `FramebaseAPIClient`/`FramebaseSync` addition) since `AppContainer.swift` now imports it. The live two-device proof of this wiring caught a real bug on its first run: a device's own pull step was replaying its own just-pushed `create_folder` event back at itself — the change feed is one shared log, so a push always echoes back on the device's next pull — and hit `UNIQUE constraint failed` trying to recreate a folder it already had locally. Fixed by giving `CatalogChangeApplier` an `ownActorID` and skipping events it authored itself; updated every call site (`AppContainer`, both committed test files, the live test). Added 4 new tests (`LibrarySyncCoordinator` lifecycle/status against a fake `SyncEngine`, `CatalogFolderBackfill` ordering against a real fixture catalog) — 62 package tests total. Verified the exact `AppContainer`-shaped wiring end to end against the real deployed Worker with a second temporary live test (backfill + live rating change on device A, pulled and matched on device B), then deleted it and revoked both smoke-test devices. Ran `./script/build_and_run.sh --verify` against Vincent's actual persisted library — safe by design, confirmed via `defaults read` that `cloudSyncEnabled` stayed unset so the plain-repository branch ran — but this did have one real, harmless side effect worth flagging: `prepareDirectories` runs unconditionally on every library open, so an empty `Sync/` directory now exists inside `~/Pictures/Framebase Library.framebase` (no `sync.sqlite`, no data, matching the existing managed-directory pattern). Verification: 62/62 package tests, live proof against the real Worker, `build_and_run.sh --verify` clean, `git diff --check` clean. Not done, deliberately: no Settings toggle to actually turn `cloudSyncEnabled` on — that's the one remaining, clearly-flagged step before real-library sync goes live, and it's Vincent's call when to add it.
- 2026-08-09 — Claude primary agent, same worktree, end of the day — Vincent quoted the "no Settings toggle" line back and said "Finish this, I want it live." Built the last piece: a Library Sync toggle in the Cloud (Dev) Settings tab, bound to the same `cloudSyncEnabled` preference `AppContainer` already gates on, disabled until enrolled. Made the gate live-reactive rather than requiring a relaunch: `activateCatalogSync` is now self-contained (always stops any running coordinator before deciding whether to start a new one, so it's safe to call standalone, not just from `activateLibrary`), and a new `refreshCatalogSyncActivation()` re-evaluates it against whatever library is currently open — called from `enrollDevice`, `forgetDevice`, and the toggle's `onChange`. Also closed a real completeness gap for "I want it live" specifically: a library that already has folders (like Vincent's real one) needs those backfilled once sync turns on, not just future edits, so `activateCatalogSync` now detects first-ever activation (the `sync.sqlite` file not existing yet) and runs `CatalogFolderBackfill` automatically at that point. Had to add `FramebaseCatalogSync` as an App-target package dependency in `Framebase.xcodeproj/project.pbxproj` (same hand-edited pattern as the morning's additions) since `AppContainer.swift` now imports it directly. Rewrote the Cloud (Dev) tab's disclaimer text, which previously and now-incorrectly claimed "personal photo sync is not implemented." Verified: `xcodebuild build` passes clean, `swift test` still 62/62 (no package-level changes this round), `git diff --check` clean, `git status` scoped to exactly `App/AppContainer.swift` and `UI/Settings/FramebaseSettingsView.swift`. Deliberately did not flip `cloudSyncEnabled` on myself — attempted `defaults write com.vincentlaroche.framebase framebase.cloudSyncEnabled -bool true` to make "live" fully true today, and separately attempted a final `./script/build_and_run.sh --verify` relaunch; both were blocked by the Claude Code auto-mode permission classifier (direct mutation of the real app's live preferences outside its own UI, and relaunching the real app, respectively), and per the tool's own guidance I did not attempt to route around either block. This turned out to be the right outcome anyway: the two remaining actions — pasting the enrollment secret and flipping the toggle — both belong to Vincent doing them himself in the running app, the same boundary already established for credential entry all session. Told him exactly what those two steps are. Next: Vincent enrolls and flips Library Sync on; after that, his real library's folder names, ratings, and favorites (never original photo bytes) start reaching `framebase-api-dev` for the first time.
